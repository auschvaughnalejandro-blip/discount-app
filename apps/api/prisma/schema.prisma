// Stage 0 — Prisma initialised only. The data model is Stage 1.
// Do not add models here ahead of Stage 1 (BUILD-PLAN §0 rule 5).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
